
 Option 3: FSA table + driver
  - Both FSA table and driver are stored in function "scanner" in "scanner.cpp"
  - Invocation with "./scanner [filename]" will read from filename.fs18 (or stdin if no filename is
    provided and return token triplet until end of file.
